fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
pyyaml>=6.0
jinja2>=3.1
httpx>=0.27
python-dotenv>=1.0
typer>=0.12
pytest>=8.0
tzdata>=2024.1
# Bayesian hyperparameter search for the crypto-perps strategy
# discovery sweep (scripts/optuna_crypto_search.py). TPE sampler +
# MedianPruner on top of the existing walk-forward harness.
optuna>=3.4

# Lint gate. La CI installe requirements.txt : sans cette ligne le step
# ``ruff check`` echoue en exit 127 avant tout pytest (cf. runs 26225708783 /
# 26225503049 du 2026-05-21).
ruff>=0.4
